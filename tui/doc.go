// Package tui holds the small set of messages shared across the
// strongo/aichat Bubble Tea chat kit's sub-packages (tui/grid, tui/transcript, tui/focus,
// tui/sidebar, tui/stream, tui/chatshell) so a leaf component (e.g. tui/grid)
// can ask the shell to do something (e.g. pin an entity to the sidebar)
// without importing the shell itself.
package tui
